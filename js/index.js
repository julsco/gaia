'use strict';

// Gaia description background text

/* const gaiaText = "GAIA helps highlights global environmental issues that pose an immediate threat or danger to ecosystems throughout the world.";
const pText = document.querySelector('.p__gaia');

const addBackground = word => {
    return `
        <span class="background__active">${word}</span>
    `
}

gaiaText.split(" ").forEach(word => {
    const pBackground = addBackground(word);
    pText.insertAdjacentHTML("beforeend", pBackground);
}); */

